# @package _global_

defaults:
  - geodro_lejepa_imagenet100ctrl_v1_optstep_accum_activation
  - _self_

metadata:
  method: geodro_raw_loss
  run_role: activation_raw_loss
  utility_mode: raw_loss

trainer:
  logger:
    tags: [geodro-v1.1, raw-loss, ablation, imagenet100ctrl, vits8, optimizer-step, accumulation, activation-pilot]
